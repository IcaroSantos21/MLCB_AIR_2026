# AULA 07 - ATIVIDADE AC-2 PARTE FINAL - VALOR DE 0,25 PONTO
# NOME: Rafael Matos Celestino RA: 136771
# NOME: Adrián Páris Guerrero Vieira RA: 137807
# NOME: Icaro Rodrigues Santos RA: 137061
# - Todos as construções desta aula, códigos e respostas, devem ficar juntas em um único arquivo chamado resultado_aula07.txt, dentro do diretório AULA_07 no repositório da dupla/trio

# ================================================================================
#           LABORATÓRIO PRÁTICO PREPARATÓRIO PARA PROVA INDIVIDUAL AAI
# ================================================================================
# Instruções:
# 1 - Complete o código Python nos blocos indicados por # TODO.
# 2 - Responda às questões conceituais ao final de cada exercício (PROIBIDO RESPOSTA POR IA NAS QUESTÕES)
# 3 - Nenhum exercício é necessário construção de código completo, só o trecho que está sendo pedido.

# --------------------------------------------------------------------------------
# EXERCÍCIO 01: Pré-processamento — Stopwords e Lematização 
# --------------------------------------------------------------------------------
# Cenário: Um e-commerce precisa limpar uma lista de tokens para remover ruídos.

tokens = ["os", "produtos", "comprados", "não", "chegaram"]
stopwords = ["os", "não"]

# TODO: Crie uma lista vazia chamada tokens_limpos
tokens_limpos = []

# TODO: Percorra cada palavra da lista 'tokens' usando um laço for
for palavra in tokens:
# - Se a palavra estiver em 'stopwords', ignore-a.
  if palavra in stopwords:
    continue
# - Se a palavra for "comprados", adicione "comprar" na lista limpa.
  elif palavra == "comprados":
    tokens_limpos.append("comprar")
# - Se a palavra for "chegaram", adicione "chegar" na lista limpa.
  elif palavra == "chegaram":
    tokens_limpos.append("chegar")
# - Para as demais palavras, adicione-as sem alteração.
  else:
    tokens_limpos.append(palavra)

# TODO: Imprima a lista tokens_limpos
print(tokens_limpos)

# Responda: Qual a diferença entre Stemming e Lematização? Por que a Lematização garante que a palavra exista no dicionário e o Stemming não?
# R: O Stemming separa as palavras da forma original que está dentro do dataset. Lematização transforma os verbos no infinitivo (forma de dicionário). O Stemming pode gerar palavras inexistentes, já a Lematização pega as palavras diretamente do dicionário.

# --------------------------------------------------------------------------------
# EXERCÍCIO 02: Validação de NLU — Threshold e Fallback
# --------------------------------------------------------------------------------
# Cenário: Um banco precisa verificar se a confiança da intenção é suficiente.

# TODO: Crie a função validar_intencao(confianca)
def validar_intencao(confianca):
# - Se a confiança for maior ou igual a 0.70, retorne "Ação Executada".
  if confianca >= 0.70:
    return "Ação Executada"
# - Caso contrário, retorne "Fallback Acionado".
  else:
    return "Fallback Acionado"

# TODO: Teste a função chamando-a com o valor 0.55 e imprima o resultado
print(validar_intencao(0.55))
# Responda: O que aconteceria de errado no sistema de um banco se o limiar de confiança (Threshold) fosse configurado em apenas 20% (0.20)?
# R: O sistema ficaria mais suscetível à erros.

# --------------------------------------------------------------------------------
# EXERCÍCIO 03: Ponderação de Palavras — Lógica TF-IDF
# --------------------------------------------------------------------------------
# Cenário: Uma imobiliária classifica mensagens com base na raridade dos termos.

# TODO: Crie a função calcular_peso_tfidf(frequencia_geral)
def calcular_peso_tfidf(frequencia_geral):
# - Se frequencia_geral for "muito_frequente", retorne 0.1
  if frequencia_geral == "muito_frequente":
    return 0.1
# - Se frequencia_geral for "pouco_frequente", retorne 0.9
  if frequencia_geral == "pouco_frequente":
    return 0.9

# TODO: Teste a função para a palavra "vazamento" ("pouco_frequente") e imprima o peso
print(f"palavra: vazamento \npeso: {calcular_peso_tfidf("pouco_frequente")}")
# Responda: Qual a lógica conceitual do TF-IDF para atribuir peso baixo a palavras muito comuns e peso alto a palavras raras num banco de dados?
# R: As palavras que são mais frequentes tem um peso menor, por ser menos "Rara". Já o peso alto, por ser menos utilizadas e mais raras.

# --------------------------------------------------------------------------------
# EXERCÍCIO 04: Métricas de Avaliação — Calculando a Precisão
# --------------------------------------------------------------------------------
# Cenário: Avaliação de um modelo de detecção de fraude em e-commerce.
vp = 80  # Verdadeiros Positivos
fp = 20  # Falsos Positivos
fn = 5   # Falsos Negativos

# TODO: Calcule a Precisão usando a fórmula: VP / (VP + FP)
precisao = vp / (vp + fp)

# TODO: Imprima a precisão formatada em porcentagem (multiplicada por 100)
print(f"Precisão: {precisao * 100}%")

# Responda: Em detecção de fraudes, o que representa um Falso Positivo e um Falso Negativo? Qual dos dois gera prejuízo financeiro direto à empresa?
#R: Criar o bloqueio de um cliente legitimo é um exemplo de Falso positivo, gerando frustração e perda de venda. O falso negativo é quando qualquer fraude passa. O que gera prejuizo financeiro é o falso negativo.

# --------------------------------------------------------------------------------
# EXERCÍCIO 05: Estruturação de NLU — Dicionário de Intenção e Entidades
# --------------------------------------------------------------------------------
# Cenário: Extrair parâmetros da frase: "Quero comprar 2 caixas de analgésico".

# TODO: Crie um dicionário chamado 'solicitacao' com as chaves:
solicitacao = {
    "intencao": "comprar_medicamento",
    "produto": "analgésico",
    "quantidade": 2
}

# TODO: Imprima o dicionário criado
print(solicitacao)
# Responda: Na arquitetura de NLU, qual a diferença conceitual entre Intenção (Intent) e Entidade (Slot)?
# R: A Intenção é uma mensagem mais genérica e direta, já a entidade é uma mensagem mais detalhada. 

# --------------------------------------------------------------------------------
# EXERCÍCIO 06: Representação Vetorial — Proximidade em Word Embeddings
# --------------------------------------------------------------------------------
# Cenário: Um sistema calcula a distância semântica entre palavras.

# TODO: Crie a função sao_sinonimos(distancia)
def sao_sinonimos(distancia):
# - Se a distância for menor que 0.20, retorne True
  if distancia < 0.20:
    return True
# - Caso contrário, retorne False
  return False

# TODO: Teste a função passando a distância 0.05 ("aluguel" e "locação") e imprima o resultado
print(f"Aluguel - Locação \nSão sinonimos: {sao_sinonimos(0.05)}")
# Responda: Por que um modelo baseado puramente em TF-IDF não consegue entender sinônimos, enquanto o Word Embeddings consegue?
#R: O TF-IDF ele busca a importancia das palavras pela frequência e não pela semelhança. Já o Word Embeddings sim, ele calcula semelhanças entre as palavras por meio de vetores densos.

# --------------------------------------------------------------------------------
# EXERCÍCIO 07: Limpeza de Texto — Tokenização, Pontuação e Stopwords
# --------------------------------------------------------------------------------
# Cenário: Higienização de mensagens em um sistema de chamados.
bruta = ["O", "sistema", "está", "muito", "lento", "hoje", "!"]
stopwords = ["O", "está"]

# TODO: Crie uma lista vazia chamada tokens_limpos
tokens_limpos = []
# TODO: Percorra a lista 'bruta' com um for e adicione à 'tokens_limpos' 
for palavra in bruta:
# apenas as palavras que NÃO estejam em 'stopwords' e que sejam diferentes de "!"
  if palavra not in stopwords and palavra != "!":
    tokens_limpos.append(palavra)  

# TODO: Imprima a lista tokens_limpos
print(tokens_limpos)

# Responda: O que são Stopwords e por que sua remoção é importante no pré-processamento?
#R: Stopwords remove palavras muito frequentes. Para encurtar o processamento da mensagem.

# --------------------------------------------------------------------------------
# EXERCÍCIO 08: Métricas de Avaliação — Calculando o Recall
# --------------------------------------------------------------------------------
# Cenário: Triagem médica automatizada em um pronto-socorro.
vp = 90  # Verdadeiros Positivos (casos graves detectados)
fn = 10  # Falsos Negativos (casos graves ignorados)

# TODO: Calcule o Recall usando a fórmula: VP / (VP + FN)
recall = vp / (vp + fn) 

# TODO: Imprima o Recall formatado em porcentagem
print(f"recall: {recall * 100}%")
# Responda: O Recall foca em evitar Falsos Positivos ou Falsos Negativos? Por que ter um 
# Recall alto é vital na área de saúde ou segurança?
#R: O recall foca em evitar falsos negativos. Pois ele filtra a porcentagem de quantas fraudes o modelo encontrou.

# --------------------------------------------------------------------------------
# EXERCÍCIO 09: Processamento Rápido — Simulando um Stemmer
# --------------------------------------------------------------------------------
# Cenário: Redução mecânica de palavras para economia de recursos.
palavras = ["correndo", "corredor", "correu"]

# TODO: Crie uma lista vazia chamada radicais
radicais = []
# TODO: Percorra a lista 'palavras' e corte os sufixos de modo que todas
for palavra in palavras:
# fiquem reduzidas ao radical "corr". Adicione "corr" na lista 'radicais'.
  if "corr" in palavra:
    radicais.append("corr")

# TODO: Imprima a lista radicais
print(radicais)
# Responda: O que caracteriza o Stemming em termos de processamento e por que ele é mais rápido do que a Lematização?
# R: O Stemming faz a redução de palavras e assim compara elas. Pois ele poda as palavras.

# --------------------------------------------------------------------------------
# EXERCÍCIO 10: Seleção de Intenção e Disparo de Fallback
# --------------------------------------------------------------------------------
# Cenário: O chatbot recebe a frase ambígua "Quero ver aquele negócio lá".
probabilidades = [0.25, 0.30, 0.15]
limiar = 0.60

# TODO: Descubra a maior probabilidade da lista usando a função max()
maior_confianca = max(probabilidades)

# TODO: Crie uma estrutura if/else:
# - Se maior_confianca for maior ou igual ao limiar, imprima "Intenção Confirmada"
if maior_confianca >= limiar:
  print("Intenção Confirmada")
# - Caso contrário, imprima "Acionar Fallback"
else:
  print("Acionar Fallback")

# Responda: Diante de uma intenção ambígua que aciona o Fallback, qual mensagem o chatbot deve exibir ao usuário e qual o objetivo dessa resposta?
#R: O chatbot deve retornar ao usuário que seja mais específico na mensagem. Entregar uma resposta mais confiável ao usuário.

